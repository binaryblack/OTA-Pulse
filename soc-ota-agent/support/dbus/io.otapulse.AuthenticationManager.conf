<!DOCTYPE busconfig PUBLIC
          "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
          "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>

  <!-- Only root can own the OTAPulse service -->
  <policy user="root">
    <allow own="io.otapulse.AuthenticationManager"/>
  </policy>

  <!-- Allow root to invoke methods on OTAPulse -->
  <policy user="root">
    <allow send_destination="io.otapulse.AuthenticationManager"/>
    <allow receive_sender="io.otapulse.AuthenticationManager"/>
  </policy>
</busconfig>

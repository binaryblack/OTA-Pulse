[Unit]
Description=SoC Monitoring Daemon
Documentation=https://github.com/example/soc-monitoring
After=network-online.target local-fs.target
Wants=network-online.target

[Service]
Type=notify
NotifyAccess=main

# Create required directories before starting
# Use -p flag and handle symlinks gracefully
ExecStartPre=/bin/mkdir -p /var/lib/memfault/coredumps
ExecStartPre=/bin/mkdir -p /var/lib/memfault/metrics
ExecStartPre=/bin/sh -c 'mkdir -p /data/ota 2>/dev/null || true'
ExecStartPre=/bin/sh -c 'if [ ! -e /var/lib/memfault/ota ]; then ln -sf /data/ota /var/lib/memfault/ota; elif [ -L /var/lib/memfault/ota ]; then mkdir -p "$(readlink -f /var/lib/memfault/ota)" 2>/dev/null || true; else mkdir -p /var/lib/memfault/ota; fi'
ExecStartPre=/bin/mkdir -p /var/log/memfault
ExecStartPre=/bin/mkdir -p /run/memfault

ExecStart=/usr/bin/memfaultd
Restart=on-failure
RestartSec=30
TimeoutStartSec=60
TimeoutStopSec=30

# Don't restart too aggressively
StartLimitIntervalSec=300
StartLimitBurst=3

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=memfaultd

[Install]
WantedBy=multi-user.target
